coté client :
    - une fonction qui initialise les grill
    - une fonction qui change la grill d'un seul player a la fois .
        Fonction qui recoit un id de player.
        Si l'id n'existe pas on creer un nouveau tableau

coté server :
    - gerer l'envoie d'une seule grill a la fois quand elle change avec son id
    - gerer l'envoie asyncrhone pour les tick
    - empecher de bouger la piece si la game n'as pas commencé